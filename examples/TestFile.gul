// Enums are the same as they are in C# BUT can extend ANY `const` type (including `const struct` etc.)
// We might at some point add an `enum class` or more accurately `enum union` to handle the Rust/Swift style enums.
enum Example : i32 {
    Mem1 = 12,
    Mem2 = 44,
    Mem4 = 55
}

func _start() -> Example {

}
